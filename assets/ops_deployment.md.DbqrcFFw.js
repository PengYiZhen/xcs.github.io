import{_ as o,o as s,c as t,af as a}from"./chunks/framework._zKnALF5.js";const g=JSON.parse('{"title":"部署说明","description":"","frontmatter":{},"headers":[],"relativePath":"ops/deployment.md","filePath":"ops/deployment.md","lastUpdated":null}'),i={name:"ops/deployment.md"};function d(l,e,n,r,c,h){return s(),t("div",null,[...e[0]||(e[0]=[a(`<h1 id="部署说明" tabindex="-1">部署说明 <a class="header-anchor" href="#部署说明" aria-label="Permalink to &quot;部署说明&quot;">​</a></h1><h2 id="生产构建" tabindex="-1">生产构建 <a class="header-anchor" href="#生产构建" aria-label="Permalink to &quot;生产构建&quot;">​</a></h2><p><strong>（服务端代码）</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">node</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dist/index.js</span></span></code></pre></div><h2 id="基础部署建议" tabindex="-1">基础部署建议 <a class="header-anchor" href="#基础部署建议" aria-label="Permalink to &quot;基础部署建议&quot;">​</a></h2><ul><li>使用 <code>pm2</code> 或容器编排管理进程</li><li>配置反向代理（Nginx）与 HTTPS</li><li>将敏感配置放入环境变量，不提交到仓库</li></ul><h2 id="健康检查与日志" tabindex="-1">健康检查与日志 <a class="header-anchor" href="#健康检查与日志" aria-label="Permalink to &quot;健康检查与日志&quot;">​</a></h2><ul><li>提供健康检查接口用于探针监控</li><li>记录关键日志：登录、房间加入、异常栈</li><li>统一日志采集，便于排障与审计</li></ul><h2 id="多实例注意事项" tabindex="-1">多实例注意事项 <a class="header-anchor" href="#多实例注意事项" aria-label="Permalink to &quot;多实例注意事项&quot;">​</a></h2><ul><li>Redis 用于共享状态与 Pub/Sub 同步（世界聊天）</li><li>所有实例需保持一致的 JWT 签名配置</li><li>建议配置连接数上限，避免单实例过载</li></ul><h2 id="人数压力测" tabindex="-1">人数压力测 <a class="header-anchor" href="#人数压力测" aria-label="Permalink to &quot;人数压力测&quot;">​</a></h2><p>上线前或扩容前，可使用专用 <strong><code>loadtest_room</code></strong> 与 <code>loadtest/concurrent-join.ts</code> 做并发连接与消息打点；生产务必关闭压测房或限制访问。详见 <a href="/ops/load-testing.html">人数压力测</a>。</p><h2 id="文档站静态资源-vitepress" tabindex="-1">文档站静态资源（VitePress） <a class="header-anchor" href="#文档站静态资源-vitepress" aria-label="Permalink to &quot;文档站静态资源（VitePress）&quot;">​</a></h2><ul><li>默认 <code>base</code> 为 <code>/</code>，打包后 HTML 会引用以 <code>/</code> 开头的资源（如 <code>/assets/...</code>），适合站点挂在<strong>域名根路径</strong>（含 <strong>GitHub Pages + 自定义域名</strong>，见下）。</li><li><strong>GitHub Pages 自定义域名</strong>：在仓库 Settings → Pages 绑定域名后，对外访问一般为 <strong><code>https://你的域名/</code></strong>（内容在根路径，<strong>没有</strong> <code>/仓库名/</code> 前缀）。此时必须把 VitePress 的 <code>base</code> 设为 <strong><code>&#39;/&#39;</code></strong>，否则 HTML 仍会请求 <code>/仓库名/assets/...</code>，在自定义域名下会 <strong>404</strong>。</li><li><strong>GitHub Pages 默认子路径</strong>（未用自定义域名、仅用 <code>https://&lt;用户&gt;.github.io/&lt;仓库名&gt;/</code>）时，必须把 <code>base</code> 设为 <strong><code>/&#39;&lt;仓库名&gt;/&#39;</code></strong>（首尾都要有 <code>/</code>）。若 <code>base</code> 仍为 <code>/</code>，会出现首页闪一下后变为站内 <strong>404</strong>（路由按域名根解析，与实际子路径不一致）。</li><li>不要用 <code>base: &#39;./&#39;</code> 代替子路径：多级目录页面上的 <code>./assets/...</code> 容易解析到错误目录。</li><li>构建产物目录：<code>docs/.vitepress/dist</code>，可用 <code>npm run docs:build</code> / <code>npm run docs:preview</code>。</li><li><strong>本地 <code>docs:dev</code></strong>：<code>base</code> 为 <code>/</code> 时从 <strong><code>http://localhost:端口/</code></strong> 打开即可。若曾使用非根 <code>base</code>（如 <code>/仓库名/</code>），须从 <strong><code>http://localhost:端口/仓库名/</code></strong> 打开，否则静态资源与 <strong>favicon</strong> 会请求到错误路径。</li></ul>`,14)])])}const u=o(i,[["render",d]]);export{g as __pageData,u as default};
